name: Build and Deploy to ECR

on:
  push:
    branches: [ "main" ]

env:
  AWS_REGION: ap-southeast-2
  ECR_REPOSITORY: chanachai.forwork/pokemon-battle
  IMAGE_TAG: v${{ github.run_number }}
  TARGET_DIR: "www/pokemon-battle-app"

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          echo "Checking files in repository..."
          ls -R
          
          echo "Moving to directory: $TARGET_DIR"
          cd $TARGET_DIR || { echo "❌ หาโฟลเดอร์ $TARGET_DIR ไม่เจอ! กรุณาเช็คบน GitHub ว่ามีโฟลเดอร์นี้ไหม"; exit 1; }
          
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .

          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

      - name: Deploy with Docker Compose
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          cd $TARGET_DIR || { echo "❌ Directory not found"; exit 1; }
          
          docker compose pull
          docker compose up -d --remove-orphans
          docker image prune -f